define(function (require, exports, module) {
  /**
   * Module dependencies
   */
  var LDFW = require("ldfw");

  /**
   * Game definition
   */
  var Game = LDFW.Game.extend();

  /**
   * Expose Game
   */
  module.exports = Game;
});
